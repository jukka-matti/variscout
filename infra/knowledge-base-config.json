{
  "description": "VariScout Foundry IQ Knowledge Base reference config",
  "apiVersion": "2025-11-01-preview",
  "knowledgeBase": {
    "name": "variscout-kb",
    "knowledgeSources": [
      {
        "name": "sharepoint-sops-ks",
        "kind": "remoteSharePoint",
        "description": "SOPs and procedures from SharePoint (on-demand, per-user permissions)"
      }
    ],
    "retrievalReasoningEffort": { "kind": "low" },
    "outputMode": "ExtractedData",
    "description": "VariScout Knowledge Base — Remote SharePoint documents with per-user access control"
  },
  "notes": {
    "sharePointKnowledgeSource": "Requires customer SharePoint connection string. Uses remoteSharePoint kind — on-demand document access with per-user permissions, no pre-indexing.",
    "llmRequirement": "Knowledge base requires an OpenAI model connection (gpt-5.4-nano recommended for cost efficiency)"
  }
}
