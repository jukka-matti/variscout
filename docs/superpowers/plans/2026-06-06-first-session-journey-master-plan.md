---
tier: ephemeral
purpose: build
title: 'First-Session Journey — Master Plan (P1–P4 → PR-FSJ-1…10)'
status: active
layer: spec
audience: human
related:
  - docs/superpowers/specs/2026-06-06-first-session-journey-design.md
---

# First-Session Journey — Master Plan

> **For agentic workers:** this is the PR-level sequencer, NOT an implementation plan. Each PR gets its own sub-plan via `superpowers:writing-plans` immediately before its build (plan-as-you-execute per `feedback_master_plan_for_multi_subsystem_specs`). Execute sub-plans with `superpowers:subagent-driven-development`.

**Spec:** [`2026-06-06-first-session-journey-design.md`](../specs/2026-06-06-first-session-journey-design.md) (active, owner-approved). Phasing authority: spec §9. Wireframes: [`docs/02-journeys/wireframes/`](../../02-journeys/wireframes/index.md) — reviewer pairs verify built interaction against the view docs (spec §11).

**Standing constraints (all PRs):** PWA before Azure mirror — the two ingest reducers are independent ~600-LOC machines; never one cross-app PR. Wizard is demoted, never deleted, until P4. Defect/wide-shaped pastes keep today's modal path until P2 (no empty-b0-under-modal interim — spec §4.2a). `--chrome` verify each PR (it keeps earning its keep). Cap ~6–8 tasks/PR.

## P1 — the landing + the project model

| PR        | Scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Gates                                                                                                                  |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **FSJ-1** | ✅ **DELIVERED 2026-06-06 via PR #307** (`488bcf70a`). **PWA landing router + Untitled project**: `ensureSessionProject` + `lib/landing.ts` (sample/`.vrs`/manual → Process tab; reconstruct-not-create; embed exempt); `DEFAULT_ACTIVE_IP_USER_ID` scope-key export; seeded Bottleneck 5-step map. Review pairs caught 2 real bugs (scope-key mismatch, stale embed closure) — fixed + negative-controlled. Chrome-verified: auto-IP + no gate + map at landing; b0 picker for map-less; embed exempt. Chrome findings → investigations: capability-modal-over-b0 (P2 input), embed chart paint (pre-existing on main).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Sub-plan: [`2026-06-06-fsj-1-landing-router-untitled-project.md`](2026-06-06-fsj-1-landing-router-untitled-project.md) |
| **FSJ-2** | ✅ **DELIVERED 2026-06-06 via PR #308** (`e97222714`). **PWA paste lands at b0** — all §7 gates closed in-PR: measurement-shaped pastes skip ColumnMapping → b0 pre-filled; wizard demoted to the "Fix data…" hatch (re-edit mode = the multi-outcome parity surface, one tap via **"+ track another outcome"**); cancel never wipes; no-Y `OutcomeNoMatchBanner` ported (chain chrome-verified end-to-end); Stage-1 gate removed (would have resurrected over the hatch) → goal ceremony = opt-in GoalBanner `startPrompt`; **§3 guarantee extended to wizard-path pastes** (`onFreshPasteAnalyzed`/`provisionPasteProject` — found via e2e, the no-Y floor was unreachable without it). The `--chrome` walk forced 4 engine/integration fixes green suites missed: date patterns vs parseFloat-prefix numeric; V8 `Date.parse` leniency ('Step 1'/'Shift 3' = dates) gated; out-of-candidates outcome degrades to unselected; live-store getters (time extraction was a spy-passing no-op). Follow-ups → investigations 2026-06-06 (b0-expander mount loop = HIGH before FSJ-4).                                                                                                                                                                                                           | Sub-plan: [`2026-06-06-fsj-2-paste-b0-wizard-demotion.md`](2026-06-06-fsj-2-paste-b0-wizard-demotion.md)               |
| **FSJ-3** | **Azure mirror — split 2026-06-06 (6-8-task cap; mirrors the PWA's FSJ-1/2 phasing): PR 0 → 3a → 3b.** **PR 0**: the b0 ProcessStepsExpander mount-loop fix (shared CanvasWorkspace, HIGH — owner call: fix BEFORE the Azure landing routes fresh entries to b0). **FSJ-3a**: landing router (sample/manual → Process tab) + Untitled hub+project pair + `useNewHubProvision` eager-persist rework (Word-style in-memory until first explicit save; first save flushes hub+document together; CreateProjectModal idempotence + embed-onto-hub fix). Sub-plan: [`2026-06-06-fsj-3a-azure-landing-untitled-pair.md`](2026-06-06-fsj-3a-azure-landing-untitled-pair.md). **FSJ-3b** (planned after 3a merges): paste→b0 + wizard demotion to "Fix data…" hatch + Azure b0Slots chrome + mapping-cancel wipe fix + goal→GoalBanner relocation (**brief relocation = minimal per owner call 2026-06-06**: brief keeps its Analyze BriefHeader + Stage-5 homes; IPDetailPage `charterFormProps` wiring → the Project-tab design session) + scoped e2e helper adaptation (~32 `confirmColumnMapping` sites; full spine rewrite stays FSJ-10). Grounding notes: Azure has NO embed mode (exemption vacuous) and the `.vrs` import UI is dead (no `onLoadProjectFile` wire) — both logged, not built. | FSJ-1+2 merged ✅; PR 0 before 3a; Editor project-open landing untouched (spec §1 applicability boundary).             |

## P2 — detections re-frame b0 (spec §4.2/§4.2a)

| PR        | Scope                                                                                                                                                                                                                                                                                                                                                           | Gates                                                                        |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| **FSJ-4** | **PWA chip/banner infra** + quiet time chip + **step-timestamps banner** (pair-regex lifts from `StepTimingsModal` pre-fill into core detection) → the shipped StepTimingsModal chain → L2 landing.                                                                                                                                                             | Wireframe `step-timings` is the contract.                                    |
| **FSJ-5** | **PWA mode re-framing**: defect banner → `analysisMode='defect'` + derived-metric picker (Count vs Rate) as b0's pinned Y (plumb `defectResult.outcomeColumn` → b0 candidate source); wide banner → `analysisMode='performance'` + `measureColumns` + **sibling exclusion** from Y/X lists + stack as secondary disposition; retire the three detection modals. | Defect confirm-sequence UI gets its wireframe BEFORE build (spec §11 + §12). |
| **FSJ-6** | **Azure mirror** of FSJ-4+5.                                                                                                                                                                                                                                                                                                                                    |                                                                              |

## P3 — the capture grammar + crossing up (spec §5/§6)

| PR        | Scope                                                                                                                                                                                                                                                                                                                                                                                   | Gates                                                                                                                                                                                              |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **FSJ-7** | **The capture card** (shared `ui` + both-app wiring): brush → card on release (mints derived factor on save; Capture / Factor-only); chart remembers (I-Chart bands); engine shift banner → compact signal chip with Capture; point-at-chart visible affordance — **incl. probability plot** (value-band condition; spec §5 amendment 2026-06-06); mobile = tap-capture + bottom sheet. | Converges with `BrushToFindingFlow` **and CCJ §3.5 inflection binning** (create-binned-factor routes through the card's Factor-only path — one grammar); wireframe `capture-card` is the contract. |
| **FSJ-8** | **Findings-forward Wall** + afterglow nudge; **promotion prompts plain-language hypothesis name**; status-ladder presentation (gestures drive via proposal chips; dropdown = override; enum untouched).                                                                                                                                                                                 | Wireframe `wall-arrival` is the contract.                                                                                                                                                          |
| **FSJ-9** | **Durability UX**: save/export nudge (own-capture rule, single-fire); `beforeunload` dirty guard; **name-before-invite** gate; **Report humanization boundary** (no raw `finding.text`/auto factor names on the executive surface).                                                                                                                                                     | Sponsor-panel amendments (spec §3/§6).                                                                                                                                                             |

## P4 — collapse + retirement (spec §8/§9)

| PR         | Scope                                                                                                                                                                                                                          | Gates                                 |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------- |
| **FSJ-10** | Writer collapse (one interactive writer + seed writers; defect-mode seam plumbed); wizard retires from the primary path (hatch remains); **E2E spine rewrite** (~25 `confirmColumnMapping` call sites — budgeted first-class). | All prior PRs merged + chrome-walked. |

## Delivery closeout (Apply phase — spec §11, per-initiative)

Rewrite `docs/02-journeys/flows/first-time.md` as the journey's L2 home (named-view links); update `pwa-solo-investigation.md` + `traceability.md` View column; OVERVIEW fixes (stale §3.0 citation, 4-verb spine); E1 T6 amendment + wedge "optional wrapper" reframe; decision-log delivery entries; investigations follow-ups (acceptInvite bug fix routing).
