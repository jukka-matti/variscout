---
title: 'Projects tab + IP detail (Lifecycle) page + Home active-IP launchpad locked'
purpose: decide
tier: card
status: active
date: 2026-05-14
topic: ['decisions', 'canvas', 'capability', 'investigation']
verified-against-commit: 6f51f080
last-verified: 2026-05-18
supersedes: []
---
> **Decision card** — extracted from `docs/decision-log.md` §1 on 2026-05-18. Aggregate view: [`decision-log.md`](../../decision-log.md) (generated).

# Projects tab + IP detail (Lifecycle) page + Home active-IP launchpad locked

Brainstorm session resolved coherence spec §15's "IP detail page details" carry-forward and reframed the top-nav structure. Six locks: (1) **7-tab nav split** — coherence's locked 6-tab Path A becomes 7 tabs (`[Home] [Process] [Analyze] [Investigation] [Improve] [Projects] [Report]`), splitting the previous single "Improvement" tab into a verb tab (Improve = legacy `ImprovementView` / PDCA workbench, zero migration) and a noun tab (Projects = new IP lifecycle + detail page); deliberate verb/noun split because the two surfaces serve genuinely different jobs (casual ideation across IPs vs. tracked-project authoring). (2) **Home becomes the active-IP launchpad** for the Project Lead persona; picking an active IP at Home cascades scope to Process / Analyze / Investigation / Improve / Report tabs via the IP-context chip (Coherence §11 pattern #5). Process Owner doesn't pick (hub-level cadence work); SME auto-scopes when accepting a consult; Frontline lives in action cards. State persists per-Hub-per-user via `localStorage` (new `useActiveIPStore` Annotation-layer Zustand store). (3) **IP detail (Lifecycle) page anatomy** — header (back-link / status pill / title / goal summary / team avatars + Invite) + stage tabs (`Charter / Approach / Sustainment / Handoff`) + Overview/Sections segmented toggle (Coherence §11 pattern #2) + 280px right rail team workspace. (4) **Approach stage is SuspectedCause-anchored** — per-cause hierarchy (Hypothesis → ImprovementIdea → ActionItem) as the visual unit in both Overview and Sections modes. Sections mode shows read-only summary with per-cause "Open in Improve workbench" jump-out (avoids duplicating the legacy PDCA tooling). (5) **Report tab IP-scoped with Overview/Technical audience toggle** (fractal pattern with IP detail's mode toggle) — Overview renders a 7-section QC-Story-shaped narrative arc with plain-English UI copy ("Where we started" / "What we aimed for" / "What we found + what we did" / "Did it work?" / "What we standardized + learned" / "What's next"); methodology lineage stays internal per RPS V1 D2 (`feedback_drop_methodology_bridges`). Technical layer renders the full analytical chart suite. Free-roaming Report = Hub-level portfolio. (6) **Single data-model addition** — optional `ImprovementProject.reflection?: string` field (sibling of `signoff`) for analyst lessons-learned narrative; backward-compatible, no `.vrs` format version bump. Spec [`docs/superpowers/specs/2026-05-14-projects-tab-design.md`](superpowers/specs/2026-05-14-projects-tab-design.md) written + committed as `2f4cff53`. Coherence spec amendments applied: §4 (nav: 6 → 7 tabs + Improve/Projects rows in table + amendment block), §15 (carry-forward marked resolved), §16 (new row in spec contributions matrix). Implementation plans next (4 sub-plans recommended: Projects tab + IP detail / Home + cascade / Team rail / Report). Tier behavior: V1 features in PWA free + Azure paid; V2 collaboration features (threaded comments, @-mentions, signoff queue UI, RACI per-section, change notifications) stay deferred + tier-gated when shipped per `feedback_tier_gate_inside_surface`. _Pinned 2026-05-14._

  **Amendment 2026-05-15 — Plan 2 PR-PT-6 shipped (PR #177).** Squash-merged as `e7794a21`, first PR of Plan 2 (`docs/superpowers/plans/2026-05-14-projects-tab-launchpad-cascade.md`). Delivered the ADR-078 Annotation-layer `useActiveIPStore` in `@variscout/stores` with per-hub/per-user `localStorage` key `variscout:activeIP:${encodeURIComponent(hubId)}:${encodeURIComponent(userId)}`; no Document-layer persistence, HubAction dispatch, Dexie table, or migration. Added shared `@variscout/ui` Active-IP primitives: Mira's Home launchpad card, presentation helpers, and the shared IP-context chip with the spec's indigo inline style. Wired PWA and Azure Home/Projects/header flows: Project Lead can start/select/switch/exit active IP, Projects list selection sets active IP before opening detail, one-IP session auto-activation honors §4.3, and Exit preserves free-roaming. Added component and app integration tests for store behavior, Home card branches, chip text/actions/style, and PWA/Azure header consumption. Verification: touched-surface Vitest suites + `pnpm docs:check` + `pnpm build` passed; `bash scripts/pr-ready-check.sh` docs failure was fixed by linking the Plan 2 plan from the Projects Tab spec. Full `@variscout/ui` suite still stalls on unchanged `Canvas.test.tsx` in this environment; isolated branch and temp-main repros point to a pre-existing Canvas/Vitest runner issue, not the PR-PT-6 diff. PR final review subagent approved after mandatory checkout STEP 0. _Amendment pinned 2026-05-15._

  **Amendment 2026-05-15 — Plan 2 PR-PT-7 shipped (PR #178).** Squash-merged as `f86ba601`, closing Plan 2's IP-context cascade. Active-IP scope now propagates across Process, Analyze, Investigation, Improve, Projects, and Report while preserving free-roaming branches and the legacy `activeView === 'improvement'` Improve tab. Shared `@variscout/ui` scope helpers derive outcome/factor/timeline labels, Process canvas focus, and lineage filters; `ActiveIPScopeRibbon` renders scoped context on the working surfaces, and Report receives an explicit header chip slot because the report workspace is full-screen. Empty lineage is intentionally empty scoped state rather than a fallback to whole-Hub findings/hypotheses/questions. PWA and Azure shells both filter scoped Investigation/Improve/Report state from `ImprovementProject.sections.investigationLineage` without mutating Document-layer data, and Analyze receives visible scope chips plus factor focus. Added Playwright coverage for Home → pick IP → chip appears → tab switching preserves chip/ribbons → Exit IP clears chip. Verification: targeted stores/ui/pwa/azure tests, PWA cascade E2E on `PLAYWRIGHT_PORT=5174`, `pnpm docs:check`, `pnpm build`, Vercel preview checks, and final review subagent approval all passed; local `pr-ready-check` still stalls only at the known full `@variscout/ui` Vitest hang after PWA/Azure suites pass. _Amendment pinned 2026-05-15._

  **Amendment 2026-05-15 — Plan 3 PR-PT-8 shipped (PR #179).** Squash-merged as `56bb4bee`, closing Plan 3's Team workspace right rail V1. The existing IP detail 280px rail now renders the V1 team roster, recent activity feed, and signoff card without shipping deferred §6.2 collaboration features. Roster entries use a shared deterministic avatar primitive; Invite persists `metadata.team[]` with role, person, and optional RACI assignment. Activity feed derivation is a pure function over explicit IP + linked Sustainment/Handoff + idea/action inputs and has isolated unit coverage. Signoff actions are gated with existing `isPaidTier()` at the action level, with Request approval / Nudge / Approve callback boundaries wired through PWA and Azure via `IMPROVEMENT_PROJECT_UPDATE`; webhook behavior remains out of scope. Responsive behavior ships per §6.3: desktop rail, tablet collapsed preference in `usePreferencesStore`, and mobile drawer opened from the IP detail header. Verification: targeted UI/stores/PWA/Azure Vitest suites, PWA Playwright happy path from `active-ip-hub.vrs`, Codex browser visual walkthrough across desktop/tablet/mobile, `pnpm docs:check`, `pnpm build`, Vercel preview checks, and final review subagent approval all passed. Local `pr-ready-check` still stalls only at the known full `@variscout/ui` Vitest runner hang after package test output; PWA E2E still logs the pre-existing React setState-during-render warning during `.vrs` fixture import. _Amendment pinned 2026-05-15._

  **Amendment 2026-05-15 — Plan 4 PR-PT-9 shipped (PR #181).** Closes Plan 4's Report tab implementation and completes the Projects tab redesign. Report now switches between active-IP scoped reporting and free-roaming Hub portfolio reporting. Active-IP reports expose the Overview / Technical segmented control; Overview renders D13's seven plain-English sections verbatim, per-cause suspected-cause cards, linked sustainment/handoff evidence, and the pure per-cause mini-chart selector. Technical renders the analytical chart suite through existing `@variscout/charts` components only, with the §7.3 methodology footnote verbatim and live chart inputs where the app shell has them. Free-roaming Report shows Hub portfolio cadence/drift/capability summaries and preserves the heterogeneous-aggregation rule by distributing when outcome specs differ. PDF export uses the required `[Hub]-[IP-title]-[Overview|Technical]-[YYYY-MM-DD].pdf` title path, and Share link is gated inside the Report surface. One-IP auto-activation now honors explicit Exit IP so the Hub portfolio remains reachable. Verification: focused core/ui/hooks/PWA/Azure tests, PWA active-IP Playwright happy path, `pnpm build`, `pnpm docs:check`, `pnpm lint` (warnings only), level-boundary and dist-integrity checks, Vercel preview checks, and final/re-review subagents passed. Local `pr-ready-check` remains limited by the known broad `@variscout/ui` Vitest hang, so impacted suites were run directly. The Projects tab redesign is fully delivered. _Amendment pinned 2026-05-15._
