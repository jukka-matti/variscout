---
title: 'first-session-journey'
description: 'First-session journey initiative — altitude model spec + wireframes home + FSJ master plan; FSJ-1+2 delivered; the product''s organizing frame'
purpose: remember
tier: card
status: active
date: 2026-06-08
topic: [memory, project]
related: []
verified-against-commit: 027927efe
last-verified: 2026-06-08
source-hash: 7f43646bb4f41e44
origin-session-id: 1a5f19e2-64ac-43a6-999b-55e8501292ee
---

> 🤖 **Generated mirror** of `~/.claude/memory/project_first_session_journey.md`. Edit there, not here. Card synced by `scripts/docs/sync-memory-cards.mjs`; re-run via `pnpm docs:rebuild` (Phase 3 A4).

**First-session journey — ACTIVE initiative (spec'd + building, 2026-06-06).** Canonical: `docs/superpowers/specs/2026-06-06-first-session-journey-design.md` (active, owner-approved) + master plan `docs/superpowers/plans/2026-06-06-first-session-journey-master-plan.md` (P1–P4 → FSJ-1…10). Delivery state = `gh pr list` + the master plan, NOT here.

**Durable design calls:** the **altitude model** is the product's organizing frame (L1 System → L2 Process → L3 Step → Explore → the condition; one grammar = engine proposes via chips/banners, analyst decides; one unit = the Finding, factor-expressed; one wrapper = the **Untitled project** — auto-created in-memory on every fresh entry, Word-style durability, ceremony opt-in). **Landing rule:** no map → b0 · map → L2 · drilled → L3; fresh-entry only (existing opens + embed exempt; .vrs reconstruct-not-create, v1-envelope-only). **Concept registry** integrated the 5 framing generations (L1-3 8f = spine; CCJ drag-zones = authoring at altitude; b0 picker = the landing; wizard → "Fix data…" hatch; click assigns role / drag assigns place). **§4.2a:** unit-of-analysis detections re-frame b0, never bypass. **Capture grammar:** one card, four entries (pin/brush/point/engine-signal) — **amended 2026-06-06: + point-at-probability-plot** (value-band carve gesture; `FindingSource chart:'probability'` shipped; CCJ §3.5 inflection binning = the same grammar, FSJ-7 converges its create-binned-factor through the card's Factor-only path; brush stays I-Chart-only; capture-card wireframe carries it). **Report humanization boundary** + **name-before-invite** unchanged.

**Process rules adopted (owner):** wireframes-in-spec at `docs/02-journeys/wireframes/` (living, edited in place — the probability amendment propagated to `capture-card.md` same-day); reviewer pairs verify built interaction vs the view doc; per-initiative Apply at delivery. See [[specs-need-wireframes]].

**FSJ-1 DELIVERED 2026-06-06 (PR #307):** ensureSessionProject + lib/landing.ts (sample/.vrs/manual → Process tab; embed exempt) + seeded Bottleneck map. Two user-id conventions: `PWA_USER_ID 'analyst@local'` vs `DEFAULT_ACTIVE_IP_USER_ID 'local'` — do NOT unify.

**FSJ-2 DELIVERED 2026-06-06 (PR #308):** paste→b0 + wizard demotion; all §7 gates closed in-PR. Durable build lessons: (1) **the chrome walk caught 4 real bugs all-green suites missed** — engine date-classification ×2 (parseFloat eats date prefixes so the analyzeColumn datePatterns were unreachable; V8 `Date.parse('Step 1')`/`('Shift 3')` parse as DATES — factor labels vanished), a spy-passing no-op (App's `useDataIngestion` getters were render-stale closures; `applyTimeExtraction` called-with-right-args but applied to `[]` — **spy-call assertions ≠ effect assertions**), and invisible-wrong-Y at b0 (store outcome ∉ candidates → degrade to unselected). (2) **De-vestibuling exposes engine quality directly** — the wizard had been masking misinference; FSJ-4/5 banner work inherits this bar. (3) The §3 Untitled guarantee needed BOTH hook callbacks (`onFreshPasteLanded` route + `onFreshPasteAnalyzed` provision-no-route) — the e2e rewrite surfaced that wizard-path pastes had no IP, making the no-Y floor unreachable; an e2e test that "documents what actually happens" can be documenting the bug. (4) An implementer's fixture workaround note ("needed Mon DD YYYY dates") IS a bug report — read workarounds as signals.

**FSJ-3 COMPLETE 2026-06-06 (PR 0 #312 + 3a #313 + 3b #314 — split per the 6-8-task cap, mirroring PWA phasing).** **PR 0:** the b0-expander mount loop's logged suspect (hydrateCanvasDocument) was WRONG — live stack capture (patched console.error grabs its own `new Error().stack` when React logs max-update-depth; the stack contains the setState caller) pinned `useCanvasHypothesisDrawing`'s reset effect: whole-object `drawTool` dep × unmemoized hook result × non-bailing `reset()` — the loop fires on ANY Canvas mount, b0-expander was just the only live mount path. Composition regression test (both hooks in ONE component — the prior suites rendered them separately, structurally hiding it; pre-fix it OOM'd the vitest worker). **3a (Azure landing + Untitled pair, Word-style):** `unsavedHubsStore` + catalog∪unsaved merge (Editor+Dashboard) + `commitHubChange` + first-explicit-save flush; `ensureHubProject`/`activateHubProject` in `apps/azure/src/lib/landing.ts`; Azure = ONE user-id convention (EasyAuth email = ACL + activeIP scope key); `useActiveIPContext` derives activeIP from `hub.improvementProject` (store-only upserts strand the gate — the shipped CreateProjectModal had exactly that embed gap, fixed + made IM-0a-idempotent). **The walk caught the 3rd FSJ stale-closure bug**: `setProcessHubId` spread the render-closure `processContext`, clobbering the map `loadSample` seeded WITHIN THE SAME INVOCATION (deps arrays can't catch mid-invocation staleness) → `bindProcessHubId` reads the live store at call time. **3b (paste→b0 + wizard demotion):** `landsAtB0` with AZURE's gates kept (ANY isDefectFormat — no PWA high|medium filter; preserves today's routing); §3 guarantee at paste-time (both callbacks); `HubCreationFlow` DELETED (post-Stage-1 pure pass-through, −366 lines); brief = minimal relocation per owner (BriefHeader/Stage-5 homes; charterFormProps wiring → Project-tab session); GoalBanner startPrompt on the frame tab via commitHubChange. **First FSJ walk with ZERO new bugs.** Method notes: vite module identity in walk probes — app-local files = `/src/...` URLs, out-of-root packages = `/@fs/...` (an @fs probe of an in-root file mints a SECOND store instance → false-empty reads); fixer subagents need an explicit verify-your-branch guard (one Haiku fixer committed to main's checkout — caught + reset before push); Azure deploy is workflow_dispatch-ONLY (push-to-main deploys nothing; billing untouched by merges). Next = **FSJ-4** (PWA chips/banners; the L1-at-b0 expander-level investigation is its lead-in).

**P2 COMPLETE 2026-06-06/07 via the Codex full-autonomy trial** (see [[codex-implements-claude-orchestrates]] for the mode + trial log): FSJ-4 PR #316 (chips + step-timestamps banner; ALSO fixed the L1-at-b0 expander bug with a captured mechanism) · FSJ-5 PR #317 (PWA mode re-framing; defect-confirm wireframe authored first; `deriveB0ModeCandidates` core helper) · FSJ-6 PR #318 (Azure mirror; silent wide auto-apply retired). Codex writes its own FSJ-format sub-plans; Claude skims merges only to ground the next prompt; **the full retrospective (live walks vs all wireframe contracts + paired review + FSJ-3-baseline comparison) runs at END of master plan** per owner. FSJ-7 (capture card) in flight; FSJ-8 prompt pre-staged. Delivery state = `gh pr list` + the master plan, NOT here.

**Method notes:** CDP synthetic clicks can fail silently — drive walks via `element.click()` in javascript_tool. `pnpm --filter X dev -- --port N` passes `--` literally to vite (binds default port) — use `pnpm --filter X exec vite --port N`. PWA e2e: port conflicts → `PLAYWRIGHT_PORT=5174` works. Walk-probe module identity: app-local files = `/src/...` vite URLs; out-of-root packages = `/@fs/...` (an @fs probe of an in-root file mints a second store instance → false-empty reads).
