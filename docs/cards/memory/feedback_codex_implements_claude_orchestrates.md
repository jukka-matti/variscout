---
title: 'codex-implements-claude-orchestrates'
description: 'The owner-approved hybrid dispatch mode: codex exec as the implementer engine inside the existing Claude plan/review/walk/merge pipeline — piloted successfully 2026-06-06 (PR #315)'
purpose: remember
tier: card
status: active
date: 2026-06-08
topic: [memory, feedback]
related: []
verified-against-commit: 027927efe
last-verified: 2026-06-08
source-hash: af8c4e1329f4c41a
origin-session-id: 15d9cabf-e1a9-4af7-bc37-5659cbc150c7
---

> 🤖 **Generated mirror** of `~/.claude/memory/feedback_codex_implements_claude_orchestrates.md`. Edit there, not here. Card synced by `scripts/docs/sync-memory-cards.mjs`; re-run via `pnpm docs:rebuild` (Phase 3 A4).

**Codex-implements / Claude-orchestrates is an approved dispatch mode** (owner, 2026-06-06; pilot = PR #315, the embed framing-toolbar leak — first Codex-implemented commit in the repo, zero review fixes needed).

**Why:** the owner wants Codex doing sub-plan implementation with Claude orchestrating. Cross-model review is a feature, not a workaround — Claude's paired spec+quality reviewers stay mandatory on Codex output (diverse failure modes).

**How to apply:**
- Dispatch: `codex exec -s workspace-write --color never "<prompt>"` with cwd in the task's own `.worktrees/<branch>/` (or `-C <worktree>`), background via Bash. CLI ≥0.137 (was an **npm global**, not brew: `npm install -g @openai/codex@latest`). Subagents work inside `codex exec` (built-in default/worker/explorer; custom TOML in project `.codex/agents/`).
- Prompt template = the same implementer shape used for Claude subagents: AGENTS.md onboarding first → BRANCH GUARD (verify `git rev-parse --abbrev-ref HEAD` before commit — wrong-tree incident 2026-06-06) → TDD steps with failing-test-first → targeted runs <90s → commit message given verbatim → structured report (DONE/BLOCKED + SHA + counts + the exact change).
- Claude keeps: grounding workflows, sub-plan writing, paired reviews, fix-routing (`codex exec resume` continues a session), the chrome walk, PR/merge, Apply-phase docs.
- AGENTS.md is Codex's onboarding and was drift-fixed 2026-06-06 (9 stores/View×1; **merge-not-squash** — it had said squash-by-default against `feedback_preserve_commit_history`; branch guard; P5 vocabulary). Keep it consistent with CLAUDE.md whenever workflow rules change.
- Codex app Computer Use (browser/native-app driving) is app-only, NOT CLI, and docs say **EEA excluded at launch** (owner is in Finland — verify in-app before relying on it). The Claude-in-Chrome walk remains the live-verification gate either way.

**Right-sizing:** well-specified TDD tasks (1–3 files, full spec) are ideal Codex dispatches; judgment-dense integration/debugging stays with Claude subagents (Opus) until Codex earns trust there. Token note: the pilot's one-line-fix task cost Codex ~187k tokens — comparable to a Sonnet implementer dispatch.

**APP-MODE PHASE HANDOFFS (owner direction 2026-06-06): the owner drives the Codex APP per master-plan piece/phase, giving Codex whole-slice implementation responsibility; Claude keeps orchestration (sub-plan, review, walk, merge).** Prior phase-level Codex history and its lessons (all in this repo's memory):
- 2026-05-04 canvas migration PR1-3: shipped, but shared-checkout collisions → [[one-worktree-per-agent]].
- 2026-05-09/10 RPS V1 (PR-RPS-1/6): shipped; review caught a real D15 violation (silent migration shim) → strict-assert feedback.
- ~2026-05-2x R6e storage: **3 parallel codex/r6e-* worktree attempts ALL superseded by main and discarded** (evaluated 2026-06-03, zero unmerged value) — the failure was overlapping scope + no mid-step gate, not code quality ([[retrospective-review-pattern]]: "the agent kept going because the path was clear and there was no mid-step gate").
**Guardrails for app-mode slices, derived from that history:** (1) ONE phase in flight at a time — never parallel overlapping Codex attempts; (2) Codex's stop-line is "branch pushed, PR opened" — merge stays with Claude's gates (paired review + chrome walk caught real defects on every FSJ PR); (3) the per-PR sub-plan is the handoff contract — Claude writes it (grounded) BEFORE the app gets the phase; the FSJ sub-plans repeatedly corrected stale spec claims an unguided agent would have inherited; (4) Codex starts each phase from AGENTS.md session-start (own worktree + branch guard).

**FULL-AUTONOMY TRIAL (owner override, 2026-06-06, starting FSJ-4):** the owner is testing Codex end-to-end WITHOUT gates 1+2 — Codex grounds, plans, builds, AND MERGES (pr-ready-check green = the only blocking gate; merge-not-squash; worktree left for audit; PR body must carry a live-verification checklist + OWNER-CALL-PENDING items). Claude's gates run as **post-merge retrospective** per [[retrospective-review-pattern]]: paired review of the merged diff + the chrome walk against the wireframe contract; findings → fix-forward PRs. Safe because deploys are manual-only (merges touch nothing live). Measure each phase against the FSJ-3 baseline (plan quality, defects found retrospectively vs at-gate) and record; if retros keep finding gate-preventable defects, renegotiate the stop-line with the owner.

**Trial log:** FSJ-7 (capture card, P3, the largest + first cross-app phase) handed to Codex 2026-06-07 — IN FLIGHT, may self-split into 7a/7b sequential PRs per the task cap; convergence mandates: BrushToFindingFlow + CCJ §3.5 binning through the card's Factor-only path. FSJ-8 prompt PRE-STAGED same day (delivered in-chat before FSJ-7 merged — its step-0 instructs Codex to ground against FSJ-7's MERGED surface, substituting for Claude's skim; do not start FSJ-8 until FSJ-7 incl. any split PR is fully merged). End-of-plan retrospective backlog: live checklists for FSJ-4/5/6 (+7/8 when merged), the defect-confirm inline-expansion OWNER-CALL, the cross-app L2-pin, dead-but-exported modal components. FSJ-6 MERGED (PR #318, 2026-06-06) — **P2 complete**: Azure routing flip (defect/wide → b0 banners), the silent performance auto-apply retired with the behavior change explicitly documented, modal mounts retired, e2e updated only where directly broken, adversarial subagent pass + green gate. FSJ-5 MERGED (PR #317, 2026-06-06): wireframe-gate honored FIRST (defect-confirm.md), §8 defect-seam plumbed via new core `deriveB0ModeCandidates`, PWA modal retirement at App-render level (components stay exported — cleanup note), **used the adversarial-review subagent when explicitly prompted and fixed its findings**, full-suite gate counts in the PR body. OWNER-CALL banked: defect-confirm presentation = inline expansion under the loud banner. End-check backlog: 2 phases of unexecuted live checklists + dead-but-exported modal components. FSJ-4 = first full-autonomy phase, MERGED by Codex (PR #316, 2026-06-06): wrote its own sub-plan in the FSJ format, diagnosed the L1-at-b0 mechanism with evidence (stale fit-paths persisting zoom 0.2/l1 — matched Claude's live probe), shipped core `detectStepTimestampPairs` + the quiet time chip (Year-excluded, undoable) + b0 modal suppression, updated the master plan. **Did NOT use subagents** (Codex only spawns them when explicitly asked — later prompts request an internal adversarial-review subagent pass explicitly). **Owner amended the trial: retrospective checks BATCH at the END of the master plan**, not per-phase; Claude skims each merge only to ground the next prompt. End-check flags so far: FSJ-4's L2-pin is in shared ui (changes Azure b0 behavior inside a "PWA-only" phase — likely desirable, verify); live-browser checklist items unexecuted per-phase, accumulate for the final walk.

**DEMO-READINESS CONTINUATION (owner, 2026-06-07; amended 2026-06-08):** the full-autonomy mode extends to the demo-readiness master plan (`docs/superpowers/plans/2026-06-07-demo-readiness-master-plan.md` §dispatch-protocol is the canonical protocol home). New since FSJ: (a) **browser verification joins the self-merge gates** — where the phase gate is user-facing, Codex drives the dev app (Playwright per `apps/azure/e2e/helpers.ts` patterns or scripted) and puts evidence in the PR body BEFORE merging; engine-only phases may gate on integration tests, stated in the PR; (b) **Variant B = Codex authors its own sub-plan as Task 0 then proceeds straight to build** — the plan-review STOP was **RETIRED 2026-06-08 (owner call)**: it had been a no-op since the FSJ trial (the owner merged L-2…L-5 each as a single "done", never pausing for review), so the protocol now matches practice — ground→plan→build→self-merge→merge, fully autonomous; L-1 sub-plan = format exemplar; (c) headless `codex exec` stalled silently on the b0 dispatch (75 min, 0 output, 0 commits, 2026-06-07) — prefer the owner-driven app; if exec is used, watch for early commits as a liveness signal. Claude post-merge review stays async/batched (its fix-count = the trial metric); for design-heavy/demo-bar phases (e.g. CS-P2) the post-merge review reads the self-authored sub-plan against the spec to catch a misread. **Orchestrator note: don't paste a "stop for plan review" line into prompts — it contradicts the retired gate AND don't tell Codex to re-run an already-merged phase (check `gh pr list` before dispatching; the CS-P1 re-run misfire 2026-06-08 came from reading the plan file's existence instead of PR state).**

Related: [[one-worktree-per-agent]], [[first-session-journey]].
