---
title: 'process-ops-entity-design'
description: 'Process-as-ops extraction + ProcessHubAnalyze disposition — spec+master plan SHIPPED; ALL BUILD PHASES DELIVERED (PO-0…PO-8b via PRs #298–#306; the two launch-blocking F items DONE). Next = the first-session-journey brainstorm (queued session #5). Pickup prompt + durable calls inside.'
purpose: remember
tier: card
status: active
date: 2026-06-05
topic: [memory, project]
related: []
verified-against-commit: 7712f1edb
last-verified: 2026-06-05
source-hash: 6e2a71505a36aa4c
origin-session-id: e2413e95-2eb9-452d-843d-c7dc03df60ae
---

> 🤖 **Generated mirror** of `~/.claude/memory/project_process_ops_entity_design.md`. Edit there, not here. Card synced by `scripts/docs/sync-memory-cards.mjs`; re-run via `pnpm docs:rebuild` (Phase 3 A4).

# Process-as-ops extraction + entity disposition — ALL BUILD PHASES DELIVERED

**Canonical homes:** spec `docs/superpowers/specs/2026-06-04-process-ops-extraction-entity-disposition-design.md` (survived grounding → web research → 6-angle adversarial review; §15/§16/§17 carry the post-delivery corrections) · master plan `docs/superpowers/plans/2026-06-04-process-ops-extraction-master-plan.md` (Phase 0 + PO-1…PO-8b — **every row DELIVERED**). **Delivery state = `gh pr list` + the master plan's DELIVERED rows, NOT this file.** As of 2026-06-05: **PO-8b DELIVERED via PR #306** (`f3abf5e37` — the LAST launch-blocking F item: reload-or-branch `SaveConflictDialog` on 412 [pre-flight + silent auto-fork retired] · per-document `withDocumentSaveLock` · ETag-capture-on-load + phantom-ETag fix · merge-preserving `ProjectMetadata` [`mergeProjectMetadata` keeps Control-owned `sustainment`] · `navigator.storage.persist()`/`estimate()` · save-serialize telemetry w/ dual >50MB/>50ms trigger; **worker-marshal CUT on research grounds** — spec §17 + ADR-091 amendment). Earlier F item: **PO-8a (#305** — v1 freeze + loud hydrate-seam validation + viewState strip). The C/D/E rename+dissolution cascades: PO-7 (#304), PO-6 (#303), PO-5 (#302), PO-4 (#301), PO-3 (#300), PO-2 (#299), PO-1 (#298), PO-0 (drift-now). Related: [[connective-surface-redesign]], [[investigation-model-design]], [[wedge-v1]].

## Pickup prompt (paste into a fresh chat)

> The **process-ops extraction master plan is fully DELIVERED** (PO-0…PO-8b, PRs #298–#306; both launch-blocking F items done). **Next = the first-session-journey brainstorm (queued session #5, owner-confirmed):** PWA sample-data → Explore → first Finding capture → Analyze/Wall handoff as ONE designed journey — open with a `--chrome` live walk, owner driving (demo-critical; demos start exactly here). After that, the other 3 queued design sessions (Home launchpad / #12 Control-closure / CoScout phase+journey / Project-tab stage overviews) + the CS-P2…P5 connective-surface PRs (see [[connective-surface-redesign]]) remain. **CS-P2 starts any time** (PO-4 shipped its `ProcessStepCapabilitySource` carrier contract). Per-PR pipeline if resuming build work: grounding workflow → sub-plan (committed on-branch, linked from the master-plan row) → `superpowers:subagent-driven-development` in its own `.worktrees/<branch>` → review pair → **final adversarial Opus branch review** → `pr-ready-check` green + both app suites → `--chrome` verify on UI-touching PRs → `gh pr merge --merge --delete-branch` → DELIVERED docs flip on main.

## Durable build decisions from execution (beyond the spec)

- **Control-readiness predicate = ZERO new fields** (`packages/core/src/controlReadiness.ts`): `isControlEligible` = `ip.status==='closed'` OR live record/handoff via the **live `ControlRecord.improvementProjectId` FK** (handoffs bridge through their record's `investigationId`); `isControlled` = live record exists. The wedge stage is *derived* from `ImprovementProjectStatus` ('draft'|'active'|'closed') — no stage marker exists or is needed.
- **`ProcessHubReviewItem` deliberately NOT re-typed** (ratified deviation): it backs ~10 analyze-keyed cadence queues PO-3 deletes wholesale; the Control queue got its own `ControlReviewItem = {project, record}`. An **interim cadence bridge** (`buildControlReviewQueue` in processHub.ts, commented) dies in PO-3 — carries one accepted behavior delta (opt-out broadened to all SUSTAINMENT_STATUSES).
- **`outcomeReference.{sustainmentRecordId,controlHandoffId}` = zero-writer fossils** (3 readers, no writer — same shape as the retired `hypothesisIds`). Logged in `investigations.md`; disposition belongs to the **#12 Control-closure brainstorm**. Also vestigial post-strip: `storage.ts` reopen-tombstone branch (analyzeStatus transition can no longer fire).
- **Survey dedup moved PO-2→PO-3** (panel dies whole; no half-stripped interim). **ProjectCard "Your tasks" block shed** with the work-item layer (§3-consistent call). **`latestActivity` doesn't exist in source** — recency = `CloudProject.modified`.
- ControlRegion mounts via **`controlRegionSlot?: ReactNode` on IPDetailPage** (packages/ui must not import apps); Editor builds the slot from the active-IP cascade.
- **PO-4 carrier carries NO rows** — the per-step row channel was dead-in-prod (all 3 duck casts resolved `[]`); rows live in explicit `rowsByAnalyze` maps = the CS-P2 re-wire seam. Two CS-P2-pending dead seams logged in `investigations.md` (empty row channel; migration-suggestion columns always `[]`). The **minimal hub selector** on Dashboard is the interim B0-reachability affordance — dies at CS-P2's lift. The storage.ts reopen-tombstone was **excised** (trigger provably dead post-PO-2); reopen semantics belong to #12.

- **PO-6 durable calls (PR #303):** unification = **TRUE STORE-SWAP, never the seed-mirror** — `useFindings` seeds only at first render (no re-sync), so a mirror + store-direct writers (BrushToFindingFlow, Wall paths) = lost-write clobber; **Azure still runs the mirror** (latent seam logged in `investigations.md`; candidate fix = an Azure store-swap mirroring §4.4). `useFindings` (the hook) survives for Azure; only the PWA call site retired. The pin→Wall heal was `--chrome`-verified live. `promoteFindingAction` shipped in the store. cascadeRules EntityKind members KEPT (entities survive; only dead tables retire). `useImprovementOrchestration` (PWA) logged as orphaned.
- **PO-5 durable calls:** `groupHypothesesByStatus` (core/findings/helpers) = the ONE analyst-owned status mapping — future conclusion-chip UIs key on it; don't ship caller-less companions. Both app categorizer memos were **gate-only ceremony** (gates ≡ `hubs.length > 0`) — the spec's "2-way vs 3-way behavioral delta" was phantom. The CS-6 pin reversal is recorded loudly (connective §4.6 banner + CS-6 row + decision-log citing PR #287); revive trigger = user demand; escape hatch = a per-finding Report-view boolean. The read-only "Investigation lineage" Charter form section SURVIVES by recorded keep-decision. **Queued design session #4: "Project tab / IP-detail stage overviews"** (investigations.md) — owns the dropped stage counts (status-chips candidate), the stale "Continue in Investigation" vocabulary, the form-section rename, the duplicate charter testid.

- **PO-8b durable calls (PR #306, 2026-06-05):** the **worker-marshal serialization was CUT** (owner asked "what is best practice" → 5-agent research workflow: postMessage structured-clone is SYNCHRONOUS on the sending thread and 2-3× slower than `JSON.parse(JSON.stringify())` for plain-object graphs — marshal-then-stringify pays a serialization tax to avoid one; Surma/web.dev + Salesforce workers retrospective + V8 stringify 2× + tldraw/Excalidraw serialize main-thread). Shipped instead: main-thread save telemetry (`Document.Save.Serialize` + dual `>50MB`/`>50ms` `ReArchitectTrigger`); revive path = columnar transferable SoA or OPFS worker-owned persistence, NEVER marshal-then-stringify (spec §17 + ADR-091 amendment + decision-log). The real measured hotspot is `cloneJson` per-facet deep-clone in `buildDocumentSnapshot`, NOT the final stringify — no worker fixes it (investigations.md). **TWO conflict-copy forks existed, not one** (grounding caught the spec's single-fork framing): the timestamp pre-flight (`if(syncState?.baseStateJson)`) AND the 412 catch — the pre-flight was deleted outright (adversarially verified strictly dominated: server PUT advances analysis.json ETag + metadata.json timestamp atomically) and `getCloudModifiedDate` SURVIVES (2nd caller = loadProject's load-time detector). **The heal is a MERGE not a recompute** (owner call): `buildProjectMetadata` NEVER produces `sustainment` (Control direct-Dexie bypass owns it; ProjectCard chip reads it) — `mergeProjectMetadata` preserves it; fixed 4 pre-existing clobber sites incl. the adapter-save meta-wipe. **Phantom-412 fixed**: `saveToCloud`'s `etag: result.etag || now` fabricated a timestamp as If-Match → guaranteed false 412 (previously silently auto-forked). **Lock⟷dialog = two-phase** (the deadlock the spec never reconciled): `navigator.locks.request` holds the lock for the callback lifetime, so the dialog NEVER opens under the lock — locked attempt returns a conflict result, dialog runs lock-free, resolutions re-acquire. **Chrome-verify technique advanced**: a network-edge `fetch` interceptor returning 412/200 (+ a GET replay) drives the REAL production conflict chain end-to-end — only the network mocks, the app code is untouched; proved the full matrix incl. the critical **reload-refreshes-ETag-then-save-succeeds (no infinite 412)** loop the adversarial review demanded. Residual logged (investigations.md): Dashboard `handlePersistInvestigation` ignores `SaveProjectResult` → silent latent conflict on the non-Editor surface (low-likelihood follow-up). `loadFromCloud`/`loadBlobProject` now return `{project, etag}` (server GET already returned etag in the body).
- **PO-8a durable calls (PR #305, 2026-06-05):** the **dev-phase no-compat principle** (owner call, generalized into `feedback_wedge_v1_no_migration_no_backcompat` + spec §16 + ADR-091 Amendment + decision-log; **expiry = first customer**) CUT the spec's three-way-branch/read-only/migration-seam — what shipped: v1 shape-freeze test + **`validateDocumentSnapshot` strict-assert at the `hydrateDocumentSnapshot` seam** (the ONE point all load paths converge; the blob path had ZERO validation pre-branch) + **viewState STRIP** (session-only; `setViewState` no longer dirties). Grounding corrections: `ProcessHub.schemaVersion` = PHANTOM (spec cited `processHub.ts:433`); the ":125 newer-rejected test" rejected on SHAPE not version (`&&` short-circuit) — split, never "flip". Build catches: `EditorEmptyState.tsx` carried a DUPLICATED `LoadError` union (unified onto the hook) — found only by the gate's turbo build (apps' tsc covers tests, again). **Chrome-verify technique: monkey-patch `window.alert`/`window.prompt` to console-capture via `javascript_tool` BEFORE triggering** — dodges the CDP-wedging native dialog while proving the real chain (the PWA decline verified live this way; v2 file via in-page `File`+`DataTransfer`+change-dispatch since `file_upload` rejects host paths). PO-8b note: `loadProjectLocally` pre-hydrate `rawData.length` touch.
- **PO-7 durable calls (PR #304):** the rename was NAME-ONLY (PO-4/PO-6 had absorbed every annotation re-point); **`AnalyzeNodeMapping.investigationId` never existed** — unanimous 11-agent grounding catch; the real surface was the migration-modal entry (value IS a project id under 1:1). **joinKey-naming convention**: locals holding the possibly-synthetic Control join value (`${hub.id}:sustainment` fallback) are named `joinKey`/`bridgeJoinKeys`, never `projectId` — `improvementProjectId` is a distinct NON-renamed direct FK (shadowing hazard). Phantom-Dexie-index drop repeated (PO-6 CausalLink → PO-7 control tables; azure v17/pwa v15, no replacement index). Dead `ScopeReadAPI` deleted (PO-6 ReadAPI precedent). ipReport clause-3 masked-clause gap closed w/ an isolation test. **The adversarial doc-accuracy review caught 4 MUST-FIX a green doc-gate couldn't** — relocation docs that mine git history mis-attribute commits easily (verify hash-by-hash); journeys invent UI interactions (D1's chip "deep-link" was a fabricated mechanic). `--chrome` verify skipped for the rename-only PR (precedent: zero behavior change + traced joins + full suites).

## Method notes (durable)

- **Reverse-dependency deletion ladder beats the atomic mega-dispatch when consumers can die first** (PO-3): deleting azure → ui → core kept every intermediate commit green with standard per-task review pairs — the `feedback_atomic_sweep_one_dispatch` carve-out is only needed when a PRODUCER change breaks consumers simultaneously.
- **Chrome-verify "freezes" = check for blocking native dialogs FIRST** (PO-3): `window.prompt` (Editor Save-As) + `window.confirm` (replace-data) wedge all CDP commands — looks exactly like a renderer hang. Repro-on-main correctly classified it pre-existing, but the cause was dialogs, not a hang. Logged in `investigations.md`; future verifies need the user to dismiss them or the paths refactored to house modals.
- PO-3 grounding killed a phantom spec rationale: `buildProcessHubContext` "slims for CoScout" had ZERO live callers and the acceptance criterion ("CoScout prompt assembly tests") had no referent — zero-caller checks on "slim/keep" targets are as load-bearing as on delete targets.
- The per-PR pipeline (grounding workflow → sub-plan → subagent build → review pair → final Opus gate) caught real issues at every layer; **the final adversarial branch review is non-negotiable** — twice it caught cross-task seams the per-task targeted runs missed (stale Dashboard assertions after the re-home; after the reviewSignal shed).
- Reviewer line refs go stale fast mid-PR — sub-plans must say "locate by symbol/label, never cited line numbers."
- PO-2 grounding corrected the spec again (no stage marker; survey-dedup timing; "latestActivity" phantom) — **grounding before each PR remains load-bearing even with a hardened spec.**
- Watch `git add -A` in worktrees: lint-staged stash backups can resurrect stray files (a stale PLAN-coscout-legacy.md got swept once; caught on amend).
- **tsc-invisible optional-prop chains need hand enumeration + grep guards** (PO-5): the pin props (`onToggleProjectLineage?` etc.) were plain-typed optional callbacks across 8 files — deleting the data type breaks NONE of them; they'd survive as orphaned dead code. Deletion-cascade sub-plans must enumerate prop chains AND make the acceptance grep (not tsc) the guard.
- **Producer `sections`-literal deletions must land WITH the type deletion when the member is required** (PO-5 implementer deviation, spec-review-ratified): a literal omitting a required member fails tsc — the consumers-first ladder can't pre-shed object-literal seeds/merges the way it sheds reads.
- The replace-data `window.confirm` blocked the chrome verify AGAIN (PO-3 first hit it; PO-5's active-IP Wall/Report/Charter live checks went test-covered-only per owner call) — verifies needing the paste/replace path stay partial until those native dialogs become app modals.

- **Package-test tsc visibility is PER-PACKAGE, check tsconfig.build before assuming grep-only** (PO-6): ui's build tsconfig INCLUDES `__tests__` (unlike core/stores/hooks) — the gate's turbo build caught a scope-literal over-deletion + 2 stale positionals that the "package tests are tsc-invisible" rule had written off. Apps' tsc covers tests too. The full `pr-ready-check` turbo build is the only sweep that runs every package's build tsconfig.
- **Crashed-implementer recovery** (PO-6 Task 5, API outage at 296 tool uses): the work was uncommitted but complete — controller re-ran the sub-plan's acceptance greps + full build/suite pipeline itself, committed, then had the spec reviewer audit the 122-file diff in BOTH directions (under- AND over-deletion) with extra suspicion + the final adversarial review double-checked. Pattern held: the only escapes were the two ui-build residues above.
- Azure's pnpm filter name is `@variscout/azure-app`, NOT `@variscout/azure` (silent "No projects matched" + exit 0 — a green-looking no-op).

## Queued design sessions (spec §8 — each its own brainstorm)

1. **Home launchpad** (consumes the slimmed attention contract + the reviewSignal user-POV walkthrough — changeSignals needs unseen-semantics; capability-as-status was a retired-persona story).
2. **Control closure model #12** (consumes the re-homed region; closure vs review-rhythm; owns outcomeReference + escalation-revive + the vestigial storage.ts branch).
3. **CoScout phase + journey model** (JourneyPhase 4-verb fossil × 5-verb/7-tab/3-stage; absorbs the CoScout-context redesign).
4. **Project tab / IP-detail stage overviews** (PO-5 — dropped stage counts, stale "Continue in Investigation" vocabulary, form-section rename, duplicate charter testid).
5. **First-session journey** (owner-confirmed 2026-06-05: runs **right after PO-8b**, not gated on CS-P2…P5) — PWA sample-data → Explore → first Finding capture → Analyze/Wall handoff as ONE designed journey (owner live-walk friction; investigations.md entry 2026-06-05). Open with a `--chrome` live walk, owner driving. Demo-critical: demos start exactly here.
