---
title: 'doc-followups-post-alignment'
description: 'Apply-on-edit doc follow-ups left after the 2026-06-02 doc-alignment initiative — 12 shipped-but-undocumented surfaces (Batch 1 the 5 known + Batch 2 the 7 found by the completeness sweep), each with real code pointers + a ready-to-paste pickup prompt. Voice Input deferred.'
purpose: remember
tier: card
status: active
date: 2026-06-05
topic: [memory, project]
related: []
verified-against-commit: 7712f1edb
last-verified: 2026-06-05
source-hash: 6bafa6625918845a
origin-session-id: d7b5f55a-73f4-473f-8bac-b5fbd14d238b
---

> 🤖 **Generated mirror** of `~/.claude/memory/project_doc_followups.md`. Edit there, not here. Card synced by `scripts/docs/sync-memory-cards.mjs`; re-run via `pnpm docs:rebuild` (Phase 3 A4).

Tracked **Apply-on-edit** doc follow-ups left after the doc-alignment initiative ([[project-doc-alignment-initiative]], delivered + closed 2026-06-02). All are **shipped-but-under-documented** — none mislead, they are simply absent or thin. Not blocking; write each on the next touch of its surface. Code pointers below were grounded by a 6-agent Explore recon on 2026-06-02 (commit `3dd6ce65`); **re-verify against code before authoring** — files move ([[feedback_subagent_grounding_catches_drift]]). The canonical tracking entry is `docs/ephemeral/investigations.md` "Doc + user-journey alignment" (the Wave-6 addendum).

> ⚠️ **SCOPE SUPERSEDED for several of these — do NOT blindly "document as V1."** A later 2026-06-02 surface-vision triage + V1 re-derivation ([[connective-surface-redesign]]) re-opened whether several of these surfaces are even wedge V1. **Document-only (genuinely V1, safe to write):** CoScout Tools (#1), Survey/Inbox (#6), Document Shelf (#9, note it's preview-gated default-off), PWA mobile (#11), Admin Hub (#3); plus the pure doc-fix **Sustainment→Control drift** (#5). **Do NOT document yet — these became design questions (decide V1-vs-named-future first):** Hub Creation (#2), Process Hub views (#4), process-map state-items (#7), offline-first PWA (#10), cross-surface badges (#8, KEPT but redesign-pending), Evidence Map popout (#12, → multi-screen design). **PresentationView (Batch-1 sweep flagged it dead) was CUT** (PR #281). See [[connective-surface-redesign]] before acting on any Process/Hub/state-item/PWA-scope item.

**1. CoScout Tools** (`shipped-partial-doc`). The AI tool-calling surface: 27 tools (7 read/auto-execute + 16 action/propose-confirm + 4 team), phase/mode-gated via a typed registry. Code: `packages/core/src/ai/prompts/coScout/tools/registry.ts` (`TOOL_REGISTRY`, `getToolsForPhase`), `packages/core/src/ai/actionTools.ts` (`parseActionMarkers`, `ActionProposal`, `[ACTION:tool:{json}]` markers), Azure handlers `apps/azure/src/features/ai/{read,action,team}ToolHandlers.ts`. Overview `docs/03-features/ai/coscout.md` exists; **no dedicated tools doc** → create `docs/03-features/ai/coscout-tools.md`. Refs ADR-029 (3→27 tools), ADR-068 (cognitive redesign).

**2. Hub Creation** (`shipped-partial-doc` — **stub exists, expand it**). Azure Mode-B framing flow that provisions a `ProcessHub` after `HubGoalForm` then routes to ColumnMapping. Code: `apps/azure/src/features/hubCreation/HubCreationFlow.tsx` + `useNewHubProvision.ts` (`createHubFromGoal`), `packages/core/src/processHub.ts`, `packages/core/src/hub/extractHubName.ts`. **No dedicated `HubAction.kind`** — the flow instantiates + persists directly via `storage.saveProcessHub`. Stub: `docs/03-features/workflows/hub-creation.md` (`status: draft`, "body to be expanded").

**3. Admin Hub** (`shipped-partial-doc` — L2 exists, no L3). Tenant/deployment admin surface (health checks, troubleshooting, KB setup) — Shield icon in Settings, soft-gated by `VariScout.Admin` App Role. Code: `apps/azure/src/components/admin/AdminHub.tsx` (+ `AdminStatusTab` / `AdminKnowledgeSetup` / `AdminTroubleshootTab`), `apps/azure/src/hooks/useAdminAccess.ts`. L2 journey `docs/02-journeys/.../azure-admin-operations.md` (Flow 10) covers the persona; **no L3** → create `docs/03-features/ui/admin-hub.md` (match `health-bar.md` shape). Scope: tenant/deploy admin only, NOT per-project ACL; no backend (browser + Azure Portal links).

**4. Process Hub views** (`shipped-partial-doc` — Capability tab only). Azure-only hub-scoped tab container: `ProcessHubView` (Status + Capability tabs) wrapping `ProcessHubReviewPanel` (current process state, cadence questions/queues, control region, evidence) + `ProcessHubCapabilityTab` (production-line-glance dashboard). Code: `apps/azure/src/components/ProcessHubView.tsx` + `ProcessHubReviewPanel.tsx` + `ProcessHubCapabilityTab.tsx` (+ Cadence/Control/Evidence siblings). `docs/03-features/analysis/process-hub-capability.md` documents the Capability tab **only**; **no L3 for ProcessHubView/ReviewPanel** → create `docs/03-features/workflows/process-hub.md`.

**5. Sustainment → Control doc drift** (one **active** doc to fix). The rename propagated to control-domain code (post-#241, IM-6 era): `packages/core/src/control.ts` exports `Control*` types (`ControlCadence`, `ControlVerdict` = holding|drifting|broken|inconclusive, `ControlStatus` = pending|confirmed-sustained|drifted, `ControlHandoffStatus`, `ControlHandoffSurface` = **9** surfaces, `ControlRecord`); shared UI `packages/ui/src/components/Control/ControlForm.tsx`; Azure `apps/azure/src/components/control/ControlPanel.tsx` + `pages/Editor.control.tsx`; PWA `apps/pwa/src/components/ControlPanel.tsx`. **Active fix:** `docs/08-products/feature-parity.md:105-106` still names `SustainmentPanel`/`SustainmentRecordEditor` → repoint to the Control surfaces. **Leave historical** (archive plans, memory cards, the completed rename plan). **Nuance — the rename is types+components, NOT every identifier:** by no-migration design, blob-path segments still emit literal `sustainment` (`process-hubs/{hubId}/sustainment/records/...`) and some core functions keep the token (`evaluateSustainmentSnapshot`). Don't "fix" those — they're storage-internal/domain-language, intentional.

**Explicitly deferred (do NOT document now):** **Voice Input** — a future (~next-year) product increment per the 2026-06-02 product decision to keep V1 design simple.

## Batch 2 — found by the hardened completeness sweep (2026-06-02, 4-angle blind critic)

The 5 above were **not** the complete set. A multi-angle sweep (routes · Azure components · PWA · core+ui domains, wiring-verified + doc-grepped) found **7 more** shipped-but-undocumented surfaces. Same Apply-on-edit discipline; re-ground before authoring.

**6. Survey / Inbox (cross-phase methodology engine).** `evaluateSurvey` + a `SurveyRule` registry whose hints render as `InboxDigest` prompts in both apps' FrameView. Code: `packages/core/src/survey/{index,evaluator,inbox,wall,control,handoff}.ts`, `packages/ui/src/components/Inbox/InboxDigest.tsx`, `…/SurveyNotebookBase/`. The "Survey is the cross-phase methodology layer" concept (methodology.md §256, [[feedback_survey_cross_phase_layer]]) + the 6 V1 rule categories have **no L3 home** → `docs/03-features/workflows/survey-inbox.md`.

**7. Process-map state-item interactions.** `EvidenceSheet` (slide-in of Findings attached to a clicked process step) + `StateItemNotesDrawer` (CRUD on `ProcessStateNote`, kinds question|gemba|data-gap|decision). Code: `apps/azure/src/components/{EvidenceSheet,StateItemNotesDrawer}.tsx`, `packages/core/src/processStateNote.ts`, wired in `apps/azure/src/pages/Dashboard.tsx` → add a section to `process-maps.md` or new `docs/03-features/workflows/process-state-notes.md`.

**8. Cross-surface connection badges (Canvas Connection Journey output, [[project-canvas-connection-journey]]).** `ContextBadgesRow` + `MultiLinkPicker` link a canvas step to its IPs / Wall threads / Quick Actions / Control items. Code: `packages/ui/src/components/CrossSurface/*`, `…/Canvas/internal/CanvasStepOverlay.tsx` → `docs/03-features/ui/cross-surface-connections.md`.

**9. Document Shelf (Azure).** Upload / auto-index / filter / manage reference docs. The *retrieval* side is in `knowledge-base-search.md` but the **shelf UI** isn't. Code: `packages/ui/src/components/DocumentShelf/*`, `apps/azure/src/components/editor/PISection.tsx` → `docs/03-features/data/document-shelf.md`.

**10. Installable / offline-first PWA.** Service worker (VitePWA + workbox precache + StaleWhileRevalidate chunk cache + autoUpdate) + the live offline banner. Documented only at L5 (ADR-004) + one FAQ line. Code: `apps/pwa/vite.config.ts`, `apps/pwa/src/App.tsx`, `apps/pwa/src/lib/chunkReload.ts` → `docs/03-features/data/offline-and-install.md`. PWA-exclusive.

**11. PWA mobile (phone) Explore.** `MobileDashboard` swipe carousel (I-Chart/Boxplot/Pareto/Stats/Map) + `MobileTabBar` + "More" bottom sheet + `MobileCategorySheet` drill. Code: `apps/pwa/src/components/MobileDashboard.tsx`. **Undocumented PWA-vs-Azure divergence** (PWA `MobileDashboard` vs Azure `MobileChartCarousel.tsx` over shared `@variscout/ui` primitives) → `docs/03-features/workflows/mobile-explore.md`.

**12. Evidence Map popout window** (`?view=evidence-map` → `EvidenceMapWindow`). The third dual-monitor popout (findings + improvement are documented in analyze-to-action.md; this one isn't). **Caveat: partially wired — URL-only reachable today** (no `openEvidenceMapPopout` call-site). Code: `apps/azure/src/pages/EvidenceMapWindow.tsx`, `packages/hooks/src/usePopoutChannel.ts` → add to the popout list in `analyze-to-action.md` + record its wiring state. Lowest priority; confirm wiring first.

**Coverage confidence:** all 4 angles self-reported high confidence; together they checked ~9 routes + 28 Azure components + 22 PWA files + 34 core/ui domains, grep-confirmed against `docs/03-features`, and ruled out false positives (ShareDropdown=unwired, PresentationView=dead export, mobile carousel covered at L2). Treat this as a strong-but-not-formal "complete set" — Voice Input excluded by design.

---

## Pickup prompt (ready to paste in a future session)

> These are Apply-on-edit doc follow-ups from the doc-alignment initiative (`project_doc_followups` memory + the `investigations.md` "Doc + user-journey alignment" Wave-6 addendum). Pick up **one** of them. **First re-ground against code** (files may have moved) using the docs toolbox + Explore, then follow the **ground → author → review** pattern: write the canonical L3 doc with SDD frontmatter (`layer`/`kind`/`serves`/`implements` + `last-verified: <today>` + `verified-against-commit: <HEAD>` + an intent diagram for ui/workflow kinds), gate on `pnpm docs:check:frontmatter` + `pnpm docs:check`, then run an adversarial code-grounded review subagent before committing (it caught a real error last time). Docs commit direct-to-main. Targets: **(1) CoScout Tools** → new `docs/03-features/ai/coscout-tools.md` from `packages/core/src/ai/prompts/coScout/tools/registry.ts` (27 tools, `getToolsForPhase`, ADR-029/068). **(2) Hub Creation** → expand the stub `docs/03-features/workflows/hub-creation.md` from `apps/azure/src/features/hubCreation/`. **(3) Admin Hub** → new `docs/03-features/ui/admin-hub.md` from `apps/azure/src/components/admin/AdminHub.tsx` (match `health-bar.md`). **(4) Process Hub views** → new `docs/03-features/workflows/process-hub.md` from `apps/azure/src/components/ProcessHubView.tsx` + `ProcessHubReviewPanel.tsx`. **(5) Control drift** → fix `docs/08-products/feature-parity.md:105-106` (`SustainmentPanel`/`SustainmentRecordEditor` → the `Control*` surfaces in `control.ts` / `components/control/`), but DO NOT touch the intentional `sustainment` blob-path segments or `evaluateSustainmentSnapshot`. **Batch 2** adds 7 more (see the list above): (6) Survey/Inbox → `survey-inbox.md`, (7) process-map state-items → `process-state-notes.md`, (8) cross-surface badges → `ui/cross-surface-connections.md`, (9) Document Shelf → `data/document-shelf.md`, (10) offline/install → `data/offline-and-install.md`, (11) PWA mobile Explore → `workflows/mobile-explore.md`, (12) Evidence Map popout (confirm wiring first) → add to `analyze-to-action.md`. Do **not** document Voice Input (deferred).
