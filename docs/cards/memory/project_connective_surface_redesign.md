---
title: 'connective-surface-redesign'
description: 'Design initiative surfaced by the 2026-06-02 surface-vision triage — the new investigation model (IM/FE scope+cause, unified canvas) never holistically designed its CONNECTIVE navigation or its surface boundaries; the Process tab especially is an accretion of ~6-8 old-model initiatives. Includes the keep/cut/redesign triage outcomes + a brainstorm pickup prompt.'
purpose: remember
tier: card
status: active
date: 2026-06-05
topic: [memory, project]
related: []
verified-against-commit: 7712f1edb
last-verified: 2026-06-05
source-hash: 28e9929b799f479e
origin-session-id: d7b5f55a-73f4-473f-8bac-b5fbd14d238b
---

> 🤖 **Generated mirror** of `~/.claude/memory/project_connective_surface_redesign.md`. Edit there, not here. Card synced by `scripts/docs/sync-memory-cards.mjs`; re-run via `pnpm docs:rebuild` (Phase 3 A4).

> **DELIVERED 2026-06-02 → spec + master plan (pushed `20b45404`).** Brainstorm→spec complete. Canonical: `docs/superpowers/specs/2026-06-02-connective-surface-model-design.md` + `docs/superpowers/plans/2026-06-02-connective-surface-model-master-plan.md` (16 PRs / 3 phases). **Settled** (supersedes the pickup-prompt below): V1 boundary (Decision 0 — process-as-ops → named-future, executed by a separate follow-up); **Model A linked-panels spine + Model B reasoning canvas** (whole-app adaptive canvas C **rejected** — too dense for a laptop, corroborated by a 5-cluster benchmark: Hex/Figma both reversed free-canvas/floating-panels; ~2–3 co-visible-surface ceiling); the **investigation loop** (Explore=divergent ⟷ Analyze=convergent; emergent "when"; the **crossing-back** = the one net-new transition); the **analytical flow** — best-subsets regression **guides which factors to watch** (global guide near Frame [placement = advanced-design open Q] + per-scope re-rank in Analyze), the **Finding (a `{factors+filters}` condition, found via Explore's visuals) is the unit**, the per-factor stat triad (boxplot/regression/Cp-Cpk) → see-the-chart → explicit call → typed Finding; **"tool assists, analyst decides"** (new invariant): deterministic stats = authority, CoScout (Azure) = interpretation partner, analyst owns every call — de-automated scoring (analyst-owned 5-state status; `deriveHypothesisStatus` → soft suggestion; re-ingest auto-link → analyst-confirm prompt); factor↔hypothesis is **Finding-mediated** (`CausalLink` demoted to optional overlay); cause = Hypothesis (factor = associated observable → hypothesis = mechanism → evidence). Locked: badges KEPT, PresentationView CUT. Plus **§2A Process-tab holistic design** (added 2026-06-02 `26c7475b` — the original "what IS the Process tab?" Q, finally answered): **orient→dive** (Frame → L1 are-we-capable? → L2 which-step → drill → Explore); the **per-step capability view = a connected boxplot** (the process flow IS its x-axis; light node flag + aligned/linked) with the **Values⇄Capability toggle** + **own-values harmonized spec-aware scaling** (keep real values, spec-window as the shared visual yardstick; baseline rule: two-sided spec→spec-window, one-sided/ratio→0, cycle-time→0); **two bottleneck axes — cycle-time bottleneck (a flow constraint, TOC) vs worst-capability step (the weakest link)**, never conflated, and **NO leaderboard** (the boxplot + the analyst's eye; counts→error-Pareto; time→light bottleneck flag); the per-step capability is **methodologically distinct from Explore's Values⇄Capability** (cross-investigation per-step Cpk distribution vs single-investigation subgroup stability — "are we capable?" not just "stable"); heterogeneous per-step CTQ + specs at framing (engines built — `ctqColumn`/`capabilityScope`/`calculateNodeCapability`/`computeBottleneck`; **net-new = the per-step spec-authoring UI + the cycle-time viz**); cadence/Status rollup → named-future (collapses the two-tab; it's a portfolio dashboard, not a process-map surface); per-step capability → PWA (parity). Master plan gained **Phase 2P (CS-P1…P5)**. **Model B + the Process-tab per-step view together = the bar before customer demos.** **Coherence-hardening (2026-06-02 user-POV eval, `c9fb77cd`):** the forward path works (ScopeChrome/ScopeRail shipped — the bridge landed), but 3 seam-clusters → folded 5 fixes into §2A.1a: (1) **scope-durable + IP-keyed prereq = PR-CS-0** — the drill→`ProblemStatementScope` bridge (`buildConditionFromCategoricalFilters → createProblemStatementScope`) has **zero live callers despite IM-4 reading "delivered"**; verify + wire FIRST (absorbs the IP-scope-bleed fix); (2) **§12 Q6 RESOLVED** — the global best-subsets "watch these factors" guide → the **orient view after framing** ("which factor" + "which step" = where-do-I-look-first; per-scope re-rank stays in Analyze); (3) explicit b0→b1 transition affordance; (4) terminology: **role-assignment** (framing) vs **factor-screening** (best-subsets) — never bare "factor selection"; (5) "See the data" must carry scope. **Architecture/refactor guidance (in the master plan):** macro is sound (ADR-078 9-store/3-layer, enforced `core→hooks→ui→apps`, boundary tests) — **this initiative IS the refactor** (incremental, blocking-first, 4 tiers: now-if-blocks / with-feature / own-pass / defer-til-bites); **no standalone architecture audit** — validate the scope-lifecycle architecture as step-0 of PR-CS-0's sub-plan. **STATE: design COMPLETE + coherence-checked + pushed (`c9fb77cd`); next = build PR-CS-0 fresh.** **Next = execute PR-CS-1** (orphan cleanup), then per-PR sub-plan + subagent-driven-development. **Method lesson:** grounding (many fan-outs) repeatedly corrected the memory's priors — the over-classification trap cuts both ways (things both over- and under-claimed as built); the adversarial spec-review caught a real over-claim I'd made; verify against code, don't recall.

> **CS-8 SHIPPED 2026-06-03 via PR #290** (8 commits; delivery state = `gh`). **DURABLE DESIGN DECISION (canonical home now `decision-log.md` §1, 2026-06-03) — association-strength magnitude = per-factor SEMIPARTIAL R²** (`= R²(kept) − R²(kept∖f)` kept / gain-on-add candidate; the SAME numerator `perFactorPValues` computes; raw R² so ≥0, on the "share of total spread" scale; the **effect size paired with the existing partial p**; **explicitly NON-summing** per ADR-073). It **refines LOCKED #2** (adds the effect size; still no Cp/BIC). Core helper `perFactorDeltaR2(kept, factors, index)` (pure, O(1) off the enumerated index, no new regression) → `@variscout/core/stats`; UI = ΔR² bars + value + a `model-not-a-verdict` framing line in `ModelBuilderBand`. **Deferred follow-ups:** record the ΔR² map onto the captured Finding's `modelContext` (cross-app → fold into CS-9); the GLOBAL best-subsets guide (§12 Q6 — design-note-gated, Process-tab orient view, NOT Analyze). **Two durable build lessons:** (1) **i18n is all-or-nothing** — `packages/core/src/i18n/types.ts` `MessageCatalog` is a CLOSED interface and all **32** locale catalogs must satisfy it, so adding a key = interface + en.ts + 31 locale placeholders (English is the convention for technical labels; the tsc build + the 70-test i18n suite enforce completeness; `check:i18n` needs `tsx` which isn't installed locally and isn't in pr-ready-check). The CS-8 plan under-counted this as a 1-file add → it was 33 files. (2) **Make integration tests load-bearing** — the adversarial final review caught that the first re-rank test had only ONE eligible factor in the drilled scope, so it couldn't distinguish real re-computation from "show the sole remaining factor"; fix = add a junk distractor (`Noise`) the engine must REJECT in-scope. A green test that can't fail for the right reason proves nothing. **Housekeeping STILL PENDING:** **3 `codex/r6e-*` worktrees linger** under `.worktrees/` (all merged into main, safe to `git worktree remove` — left pending "Codex not active on them" confirmation; trip the pre-commit SSOT duplicate-CLAUDE.md warning). **Method win again:** grounding before acting caught the ~70%-built over-claim + the closed-`MessageCatalog` scope; the adversarial review caught the non-discriminating test — [[feedback_subagent_grounding_catches_drift]].

> **CS-9 SHIPPED 2026-06-03 via PR #291** (6 commits + a review-fix commit; merge `e8469b32`; delivery state = `gh` + master plan). Rendered spec §4.0's "see the actual chart": the per-factor triad row now shows an inline `MiniScatterFit` (continuous) / `MiniBoxplot` (categorical), **focus-gated** (only the focused hub computes a chart). **Grounding re-sized it = same shape as CS-8** — the triad engine, regression/two-sample routing, the FE-2b disconfirmation, and run-and-attach already shipped; the only delta was the chart. **Key correction grounding caught:** the master plan's "extend `deriveMiniChartConfig`" pointed at the WRONG surface — that's the always-on `HypothesisCard` slot (**Surface A**); spec §4.0's "see the chart" is the test-plan **triad** in `HypothesisCardWithPlans` (**Surface B**, which rendered no chart). **Owner-locked scope:** Cp/Cpk **deferred** to a follow-up (no spread-question trigger exists in code — `suggestToolForFactor` never emits `capability`); **kept run-and-attach** (the explicit analyst-owned support/counts-against call is **CS-10's** de-automation, NOT pulled forward); **no new `FindingSource` variant** (evaluate findings stay source-less — and `core/CLAUDE.md`'s stale "5 variants" was corrected to 4 structural / 5 chart-discriminant values); no new i18n (hardcoded mini-chart aria-labels, like the siblings). Net-new = pure `deriveScatterFitData`/`groupOutcomeByFactor` (reuse the `getBestSingleFactor`→`predictFromUnifiedModel` OLS engine, **no new stats math**) + `MiniScatterFit` + an optional `TestPlanFactorView.chart` populated only for `focusedWallEntityId`. **Durable build/method lesson:** the **final adversarial Opus review caught a real CORRECTNESS bug** (not merely a weak test) that a green pr-ready-check + per-task reviews all missed — `deriveScatterFitData` drew the fitted line as a straight min→max chord, but the OLS engine can select a **quadratic** term (`predictFromUnifiedModel` evaluates `coef·(x−mean)²`), so a U-shaped fit rendered near-flat; fixed by **sampling the curve into a polyline** + a load-bearing curvature test (midpoint dips below the chord — the old impl fails it). Reinforces: always run the final adversarial review even when the gate is green — [[feedback_load_bearing_tests]]. **CS-8's deferred "record the ΔR² map onto the Finding's `modelContext`" follow-up was NOT folded into CS-9** (CS-9 scope = the chart) — still open. Next per master-plan sequence = **CS-10** (de-automated scoring — analyst-owned status), then CS-11/CS-12.

> **CS-10 SHIPPED 2026-06-03 via PR #292** (merge `e9f85d2c`; 15 commits across 4 tasks; delivery state = `gh` + master plan). De-automated hypothesis scoring → **analyst-owned status**. Three moves: (1) atomic compiler-guided rename `HypothesisStatus 'confirmed' → 'evidence-survived-test'` (aligns code to the already-shipped `'Supported'` label, kills the certainty overclaim); (2) re-introduced analyst-owned `setHubStatus` (store + `useHypotheses` + UI prop + both apps, mirroring `recordDisconfirmation`) — but over the **already-existing** `analyzeStore.updateHub({status})` write-path, so a "named intent action," NOT an un-delete; (3) flipped the Wall display (`WallCanvas:918`, `MobileCardList`) from `deriveHypothesisStatus(...)` to stored `hub.status`, the derivation demoted to an **advisory suggestion chip** + an analyst-set control on `HypothesisCardWithPlans` (gated `canEdit`). **Owner-locked scope (free choice + suggest-only chip; no validation gate, no contradiction warning); leave + log the pre-existing PWA(3-way)/Azure(2-way) conclusion-categorizer divergence as a parity follow-up** (logged in investigations.md); re-ingest auto-link = CS-11, CoScout = CS-14 — untouched. **Grounding re-shaped it again (same pattern as CS-8/CS-9):** `deriveHypothesisStatus` was already pure/render-only (no auto-write to neutralize — the "demotion" is structural); **reload-durability is FREE** (DocumentSnapshot round-trips `hypotheses[]` wholesale → **no IDB migration**); the value rename touched **zero i18n catalogs** (catalog key independent of the enum value); the flip **revived ~8 dead `h.status` readers** (behavior change, tested). **Corrections recorded:** the setter was deleted in **IM-4a** (`84045c42`), NOT IM-4c (spec/master-plan had it wrong; comment fixed); the one genuinely **silent** rename site was `charts/EvidenceMap/SynthesisLayer.tsx`'s loose-typed `getStatusColor(status?: string)` (tsc couldn't catch it — hand-fixed + its `CONFIRMED/NOT CONFIRMED` overclaim labels softened to `SUPPORTED/REFUTED`). New chip/control i18n keys WERE a 32-catalog add (completeness-enforced). Final adversarial Opus review: **READY — YES**, no Critical/Important; precedence contract (analyst value wins, derivation never displayed/written, no `proposed`→derived fallback) verified at every site; negative controls confirmed load-bearing (analyst value set to one the derivation would NOT produce → fails under revert). Closed the IM-4a "stored-vs-derived hub.status split" investigations prior via the **third path** (analyst-set stored is SoT; derivation advisory). **Housekeeping — codex worktrees RESOLVED + REMOVED (2026-06-03).** The **3 `codex/r6e-*` worktrees** (`r6e-azure-storage-docs` / `r6e-same-origin-storage-b` / `r6e-storage-boundary-c`) carried uncommitted Codex R6e storage WIP on local-only branches (never pushed, based on `f1af04a2`). A 3-agent evaluation fan-out + controller verification proved **ALL THREE are SUPERSEDED by main commit `d9eb2bc5` "Implement R6e server storage boundary"** — which landed the full R6e same-origin storage boundary (client `blobClient.ts` + a 714-line `server.js` + tests + all 24 docs) more completely than the parallel Codex attempts: `-c`'s `storage.ts`/tests are byte-identical to main (its one differing file is *behind*), `-b`'s `blobClient` is a client-only half with an *incompatible* endpoint contract (no `server.js`), and the docs slice is a *lesser draft* of what main already has (net-negative diff). Zero unmerged value → **removed all 3 worktrees + deleted the branches** (`git worktree remove --force` + `git branch -D`); also swept stale `.claude-flow/` scratch dirs (`feat`/`framing-layer-v1-slice-2/3`). `.worktrees/` now holds only `_review-harness.js` (a deliberate reusable adversarial-review workflow). The SSOT duplicate-CLAUDE.md pre-commit warning is **GONE**. Method note: "look at the target before deleting" caught that the earlier "merged, safe to remove" note was wrong (uncommitted WIP) — evaluated rather than blind-removed; evaluation then confirmed safe-to-discard. Next per master-plan sequence = **CS-11**

> **CS-11 SHIPPED 2026-06-03 via PR #293** (merge `f694c8c2`; 11 commits / 7 tasks; delivery state = `gh` + master plan). **Re-ingest de-automation:** the engine now emits pure `ReingestPendingMatch` descriptors — the **THREE silent writes** (grounding correction: spec counted two) are gone: the source-less auto-Finding (which was `validationStatus:'inconclusive'`, NOT `status` — injected via direct `setState`, not a HubAction), the `MEASUREMENT_PLAN_LINK_FINDING` dispatch, and the `'in-progress'` auto-bump. Mechanical `matchColumnsToPlans` preserved verbatim; REPLACE telemetry branch byte-identical; §12 Q5 cascade still deferred. **Owner design (the user asked for context-grounded design):** **"hints navigate, chips apply"** — the match fires at *import time* (analyst on the ingestion surface) but the work happens *on the Wall*, so the Wall `MeasurementPlanChip` is the SINGLE apply surface (Link finding… / Mark in-progress / session-only dismiss) + a navigate-only Inbox breadcrumb (landed in BOTH FrameViews — no parity gap) routing to Analyze with the hypothesis focused (CS-5 path); ingest match-summary line = logged polish. **Owner picked (on my recommendation) the FULL analyst-owned 4-state plan-status select** (CS-10 pattern re-applied) over minimal — key grounding find: `onEditPlan` was a V2 stub and the auto-bump was the SOLE producer of `in-progress` (complete/skipped had NO producer), so removing it without a setter would freeze plans at `planned`; the select closes that dead-end pre-demos. **Cluster de-rank:** `detectEvidenceClusters` groups first-seen without `rSquaredAdj`/sort; `SynthesisPrompt` drops the R²% (grouping + "Name this cause" CTA kept; `modelBuilder`/`ModelBuilderBand` = CS-8's legitimate ΔR² surface, untouched; Azure-only, parity logged). **`onPlansChanged` nonce relocated** to the analyst's manual writes (status-set AND link-confirm, both apps). Final adversarial review READY — zero-silent-write traced end-to-end; the WallCanvas seam test was **mutation-verified** load-bearing; one Minor (stale 3-arg test calls, hidden because tsconfig.build excludes `__tests__` from tsc — a gate-posture fact worth remembering) fixed in-branch. i18n: only 5 new keys (reused the existing `wall.collect.status.*` labels; Inbox strings are app-layer plain strings per the survey-prompt convention). Next per master-plan sequence = **CS-12** (the reasoning canvas — the centerpiece; its sub-plan WILL sub-slice 12a/12b/12c), then CS-13/CS-14. (re-ingest auto-link → analyst-confirm prompt + cluster grouping — IN GROUNDING 2026-06-03), then CS-12 (the reasoning canvas). **Initiative follow-ups now tracked** in master-plan §"Tracked follow-ups" + `investigations.md`: ΔR²→`Finding.modelContext` (CS-8), PWA/Azure categorizer parity (CS-10), non-en locale retranslation, optional laptop visual glances.

> **CS-13 SHIPPED 2026-06-04 via PR #297** (merge `fc42d0b2`; 11 commits / 7 tasks; delivery state = `gh` + master plan). **The crossing-back (spec §4.0a):** → jump buttons on the focused `FactorGlyph` + `HypothesisCard` header → Explore lands scoped to the entity's **local y=f(x)** via `navigateToExploreForChip` (factor target gained optional `outcomeColumn`). **Owner-locked durable calls:** hypothesis carries **y+x ONLY** — `categoricalFilters` are chip-display-only (no Explore chart reads them, no inverse `ConditionLeaf[]→categoricalFilters` converter exists; writing the WHERE would make the chip claim scope the charts don't honor → logged CS-3b-adjacent follow-up in `investigations.md`); **data-presence gate** = *"explore what exists; plan what doesn't"* (no → when factor ∉ `activeColumns`; the CS-11 plan chip owns awaiting-collection; the gate composes with re-ingest §4.5 — post-confirm the → lights up); the Azure Evidence-Map drill **composes** `handleExploreFactor` (was unscoped `setHighlightedFactor`+`showExplore` — `highlightedFactor` is a PI scroll hint, NOT chart scope); PWA = chip-parity only (**grounding correction worth keeping: the PWA Explore charts read NOTHING from `analysisScopeStore`** — only the chrome `PersistentScopeChip`; `DEFERRED(lv1-pwa-mount)`; Azure's mirror is `Dashboard.tsx` yColumn+boxplotFactor only, `stepId` inert `TODO(lv1-e-step-mirror)`). **Method/build lessons:** (1) the final adversarial Opus review caught what green vitest + per-task reviews missed AGAIN — tsc CFA narrows a captured-props ref to `null` at an in-test inline reset (`render()` opaque), breaking both apps' builds; the `tsconfig.build`-excludes-tests posture from CS-11 does NOT apply to apps (their tsc covers tests) — apps' builds ARE the test-file type gate. (2) Per-task quality reviews caught two real pre-merge issues: SVG-lane collision (card → vs the `hasGap` badge — re-laned x:216–242) and duplicate-vs-compose on the drill retrofit. (3) The Wall's interactive-affordance idiom for SVG `role="button"` hosts = `OneStepAwayBadge`'s `foreignObject`+HTML-button+stopPropagation (FactorGlyph root is `<g role="button">` — never nest; glyph explore callbacks carry the RAW `factorKey`, never the `factor:`-namespaced DOI id). Live `--chrome`: full gesture verified (glyph → → Explore + chip `Fill_Weight_g · Line`; gate live; no viewBox clipping); the in-card "+ Add condition" gesture didn't open an editor in 2 attempts — pre-existing surface, noted in the build record. Next per master-plan = **CS-14** (CoScout interpretation partner) + Phase 2P (CS-P2 per-step boxplot is the other demo-bar half).

A **design initiative** that surfaced while triaging shipped surfaces against the wedge V1 vision (2026-06-02). The triage's real finding wasn't a cut-list — it was that **the IM/FE work built the new investigation _core_ (scope=`ProblemStatementScope`/cause=`Hypothesis`, unified canvas, always-on charts) but never holistically designed the _connective tissue_**: how you move between surfaces, and what each surface _is_ in V1. Several shipped surfaces are **partial, old-model answers to that un-designed question** — so "keep or cut" was the wrong frame for them; "redesign for the new approach" is right.

**The design target (user's words + my synthesis):**
- **Connective navigation** — concrete spine the owner asked for: _"from any process step, see what's attached (its linked findings), and follow the thread to where the thinking lives (jump to Analyze → the hypothesis)."_ Generalizes to: every entity should have a clear **"where did this come from / where does it go"** path across **Frame ↔ Explore ↔ Analyze ↔ Improve ↔ Control**.
- **Multi-screen** — Process view + Analysis Wall + Explore on **separate screens, kept in sync** (the owner raised this). The popout infra (`usePopoutChannel` + `BroadcastChannel` + `HYDRATION_KEYS`; the wired findings/improvement popouts) is the seed.
- **The Process tab is likely an accretion, not a holistic design** (owner's key realization). It piled up across ~6-8 initiatives at different times: Layered Process View (3-band Outcome/Flow/Operations), Process Hub (cadence + Status/Capability tabs), Production-Line-Glance (Capability dashboard), Framing Layer (hub creation + SIPOC), Canvas/8f viewport, Canvas Connection Journey (cross-surface badges), RPS state-items/notes/response-paths. **"What IS the Process tab in V1, holistically"** is probably the centerpiece question.

**DEEPER REFRAME (owner, 2026-06-02): the V1 boundary itself is under-examined — start by re-deriving V1, not from the surface list.** "V1" is not a ground-up design; it's the **wedge pivot (2026-05-16) boundary drawn _over_ ~6 weeks of pre-wedge accreted code** (4-persona/multi-hub-portfolio/tiered Product Vision 2026-05-03; the continuous process-owner operating model 2026-04-27; Framing Layer / Process Hub / Production-Line-Glance / RPS / Canvas). The pivot set strategy+nav and IM/FE rebuilt the investigation core, but never went surface-by-surface — so "the V1 surface" ≈ "what was built minus the explicit cuts," which is why every "is this V1?" keeps recurring. **The owner re-opened 3 of the "solid V1" surfaces** (hub-creation, process-map state-items, offline-first PWA) — and they all lean the OTHER way.

**The single clarifying call (my synthesis, owner to confirm):** derive surfaces from the wedge value prop = _"one improvement specialist runs one investigation → improvement → control, optionally with a small team, worth €120/mo."_ Then **the murky layer is one thing: "Process-as-ongoing-operations"** (Process **Hub**, hub creation, current-state/state-items, cadence monitoring) = the **process-owner continuous-operations model = VariScout Process (named-future)**, pulled into V1 only because the code existed + the nav has a "Process" tab. **Candidate resolution:** Process tab in V1 = the process **map** (SIPOC/steps/outcomes/specs → frames Explore/Analyze); Process **Hub**/owner-monitoring = named-future; hub-creation reframes to "investigation setup"; state-items+cadence → VariScout Process; the **free PWA is a separable funnel bet**, not core V1. Clearly-V1 spine: Project+collaboration · Explore · Analyze/Wall · Improve · Control · Report · CoScout.

**Scope (was "3 blue surfaces + 2 threads" — now nested under the V1 re-derivation).** Connective-navigation surfaces: cross-surface badges · Evidence Map popout/multi-screen. Process-tab identity + the process-owner-operations V1-vs-named-future call (subsumes: Process Hub Status/cadence, **hub creation, process-map state-items**). Separable product call: **offline-first PWA / free-tier scope**. (Genuinely solid-V1, document-only: CoScout Tools, Survey/Inbox, Document Shelf, PWA mobile, Admin Hub.)

**Decisions already made (2026-06-02):**
- **Cross-surface connection badges: KEPT** (reverted the cut). They implement wedge §3.3.2 "**in-flight references**" (step → active IP / Wall threads / Quick Actions / Control records, click to navigate) — a live implementation of a spec'd concept + the seed of the connective-nav redesign, not dead code. Code: `packages/ui/src/components/CrossSurface/*`, wired via `CanvasStepOverlay`. (Minor cleanups when touched: rename `ContextSurfaceType` `'sustainment'`→`'control'` label; a dead post-ADR-085 `questions:[]` branch in `CanvasStepOverlay` is genuinely dead.)
- **PresentationView: CUT** — shipped as **PR #281** (truly dead; presentation mode removed in `ee52e664`).
- **Process Hub cadence strip: NOT a mechanical delete** — `ProcessHubReviewPanel` (the Status tab) entangles the named-future cadence loop (snapshot cards + `ProcessHubCadenceQuestions`/`Queues` + `buildProcessHubCadence`) WITH three V1 things: response paths / `ProcessHubCurrentStatePanel` (coupled via `buildCurrentProcessState(rollup, cadence)`), the **Control region** (`ProcessHubControlRegion`, nested inside `ProcessHubCadenceQueues`), and **Survey/Inbox** (a keep). So "what is the Process Hub Status tab in V1" is a design decision inside this initiative, not a cut.

**Method:** this is a **brainstorm → spec** (`superpowers:brainstorming`), recommended in a **FRESH chat** (the originating chat was cleanup-heavy/long; context is recorded here + in `docs/ephemeral/investigations.md`). Honor the repo's full-vision-spec norm ([[feedback_full_vision_spec]], [[feedback_step_back_for_system_design]]).

## Pickup prompt (paste into a fresh chat)

> Let's design the **V1 surface model + connective navigation for VariScout's updated investigation approach** (see the `connective-surface-redesign` memory + the `docs/ephemeral/investigations.md` entry). Use `superpowers:brainstorming`. **START FROM FIRST PRINCIPLES, NOT THE SURFACE LIST:** "V1" today is the wedge-pivot boundary drawn over ~6 weeks of pre-wedge accreted code, never re-derived surface-by-surface — so begin by **(0) re-deriving V1 from the wedge value prop** ("one improvement specialist runs one investigation → improvement → control, optionally with a small team, worth €120/mo"). The likely-decisive call: is the **"Process-as-ongoing-operations" layer (Process Hub, hub creation, current-state/state-items, cadence monitoring) wedge V1, or VariScout Process (named-future)?** Candidate resolution to pressure-test: Process tab in V1 = the process _map_ (SIPOC/steps/outcomes/specs); Process _Hub_/owner-monitoring = named-future; hub-creation → "investigation setup"; state-items+cadence → named-future; the **free PWA is a separable funnel bet**. THEN, downstream of that: **(1) connective navigation** — spine "from a process step → its linked findings → jump to Analyze to see the hypothesis," generalized to where-from/where-to across Frame↔Explore↔Analyze↔Improve↔Control; **(2) multi-screen** Process/Wall/Explore kept in sync (build on existing `usePopoutChannel`/`BroadcastChannel` infra); **(3) the Process tab's identity** (it's an accretion of ~6-8 old-model initiatives). Ground every claim against shipped code (it's all wired). Decisions already locked: badges KEPT (wedge §3.3.2 in-flight references), PresentationView CUT (PR #281). Genuinely solid-V1 (document-only, out of scope): CoScout Tools, Survey/Inbox, Document Shelf, PWA mobile, Admin Hub.
